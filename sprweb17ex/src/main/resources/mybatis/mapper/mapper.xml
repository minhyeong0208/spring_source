<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="pack.model.DataMapInterface">
   <select id="selectSearch" parameterType="hashmap" resultType="pack.model.Jikwon">
      select * from jikwon where buser_num=#{buser_num} and jikwon_rating=#{jikwon_rating}
   </select>
</mapper>